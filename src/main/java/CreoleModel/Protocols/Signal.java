package CreoleModel.Protocols;

import CreoleModel.MessageProtocol;

public class Signal extends MessageProtocol {
    public Signal(String userName, ProtocolType ProtocolType) {
        super(userName, ProtocolType);
    }
}
